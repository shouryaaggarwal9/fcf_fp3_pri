export default function Home() {
  return (
    <>
      // We will add the Search Bar and "Add Label" button here in Phase 2
      Search items to add them to the queue... // We will add the selected print
      queue list here in Phase 2 Queue is empty // Sticky Bottom Print Bar Print
      0 Labels
    </>
  );
}
